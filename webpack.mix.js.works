let mix = require('laravel-mix');

mix.autoload({
  jquery: ['$', 'jQuery', 'window.jQuery'],
  'popper.js/dist/umd/popper.js': ['Popper']
})

mix.js('test.js', 'testoutput.js')
  .mix.sass('test.sass', 'testoutput.css')
  .mix.version()
;
